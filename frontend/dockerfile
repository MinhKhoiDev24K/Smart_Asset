FROM nginx:alpine

# Xóa file trang mẫu mặc định của nginx
RUN rm -rf /usr/share/nginx/html/*

# Copy toàn bộ frontend vào thư mục web root
COPY . /usr/share/nginx/html/

EXPOSE 80

CMD ["nginx", "-g", "daemon off;"]
